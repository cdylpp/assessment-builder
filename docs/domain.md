# Domain Rules

1. Metrics are immutable after publication.
2. Metric changes create a new metric version.
3. Evolutions are immutable after publication.
4. Evolution changes create a new evolution version.
5. Events may be draft, published, archived, or copied.
6. Export uses the exact event version selected by the user.
7. Excel generation receives YAML only; it does not query the app db.